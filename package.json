{
  "name": "ieee-website-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=frontend",
    "build": "node setup-vercel.js && npm run build:vercel --workspace=frontend",
    "build:vercel": "npm run build:vercel --workspace=frontend",
    "start": "npm run start --workspace=frontend"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "mongodb": "^6.16.0",
    "react-tsparticles": "^2.9.3",
    "tsparticles": "^2.9.3"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.3",
    "tailwindcss": "^4.1.6"
  }
}
